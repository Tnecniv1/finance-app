<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Importer des transactions - Finance App</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="dashboard">
        <!-- Header -->
        <header class="header">
            <div class="header-content">
                <h1>💰 Finance App</h1>
                <div class="user-info">
                    <span>Bonjour, <strong><%= pseudo %></strong></span>
                    <a href="/transactions" class="btn btn-small">Retour</a>
                    <a href="/auth/logout" class="btn btn-small">Déconnexion</a>
                </div>
            </div>
        </header>

        <div class="main-content">
            <!-- Messages -->
            <% if (error) { %>
                <div class="alert alert-error">
                    <%= error %>
                </div>
            <% } %>
            
            <% if (success) { %>
                <div class="alert alert-success">
                    <%= success %>
                </div>
            <% } %>

            <!-- Upload Form -->
            <div class="card">
                <h2>📥 Importer vos transactions</h2>
                <p style="color: #666; margin-bottom: 20px;">
                    Importez vos relevés bancaires au format CSV. Le fichier sera sauvegardé et traité automatiquement.
                </p>

                <form action="/transactions/import-csv" method="POST" enctype="multipart/form-data">
                    <div class="form-group">
                        <label for="csvFile">Sélectionnez votre fichier CSV *</label>
                        <input 
                            type="file" 
                            id="csvFile" 
                            name="csvFile" 
                            accept=".csv,.txt"
                            required
                            style="padding: 10px; border: 2px dashed #d1d5db; border-radius: 8px; width: 100%; cursor: pointer;"
                        >
                        <p style="color: #888; font-size: 0.9rem; margin-top: 8px;">
                            Formats acceptés : .csv, .txt • Taille max : 5 MB
                        </p>
                    </div>

                    <button type="submit" class="btn btn-primary">
                        ⬆️ Importer les transactions
                    </button>
                </form>

                <div style="margin-top: 20px;">
                    <a href="/transactions/import-csv/template" class="btn btn-secondary" download>
                        📄 Télécharger un exemple de fichier CSV
                    </a>
                </div>
            </div>

            <!-- Recent Imports History -->
            <% if (recentImports && recentImports.length > 0) { %>
            <div class="card">
                <h2>📋 Derniers imports</h2>
                <div class="imports-list">
                    <% recentImports.forEach(imp => { %>
                        <div class="import-item status-<%= imp.status %>">
                            <div class="import-icon">
                                <% if (imp.status === 'completed') { %>
                                    ✅
                                <% } else if (imp.status === 'error') { %>
                                    ❌
                                <% } else if (imp.status === 'processing') { %>
                                    ⏳
                                <% } else { %>
                                    📄
                                <% } %>
                            </div>
                            <div class="import-info">
                                <div class="import-header">
                                    <strong><%= imp.filename %></strong>
                                    <span class="import-date">
                                        <%= new Date(imp.created_at).toLocaleDateString('fr-FR', {
                                            day: '2-digit',
                                            month: 'short',
                                            hour: '2-digit',
                                            minute: '2-digit'
                                        }) %>
                                    </span>
                                </div>
                                <div class="import-stats">
                                    <% if (imp.status === 'completed') { %>
                                        <span class="stat-success">
                                            <%= imp.imported_count %> importée(s)
                                        </span>
                                        <% if (imp.error_count > 0) { %>
                                            <span class="stat-error">
                                                <%= imp.error_count %> erreur(s)
                                            </span>
                                        <% } %>
                                    <% } else if (imp.status === 'error') { %>
                                        <span class="stat-error">Erreur</span>
                                    <% } else if (imp.status === 'processing') { %>
                                        <span class="stat-pending">En cours...</span>
                                    <% } else { %>
                                        <span class="stat-pending">En attente</span>
                                    <% } %>
                                </div>
                                <% if (imp.error_details && imp.status === 'error') { %>
                                    <div class="import-error">
                                        <%= imp.error_details.split('\n')[0] %>
                                    </div>
                                <% } %>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>
            <% } %>

            <!-- Format Info -->
            <div class="card" style="background: #f0f9ff; border-left: 4px solid #3b82f6;">
                <h3 style="color: #1e40af; margin-top: 0;">📋 Format du fichier CSV</h3>
                <p style="color: #1e40af; margin-bottom: 10px;">Colonnes minimum requises :</p>
                <ul style="color: #1e40af; padding-left: 20px; line-height: 1.8;">
                    <li><strong>Date</strong> : JJ/MM/AAAA ou AAAA-MM-JJ</li>
                    <li><strong>Description</strong> : Libellé de la transaction</li>
                    <li><strong>Montant</strong> : Positif (revenu) ou négatif (dépense)</li>
                </ul>
            </div>

            <!-- Bank Guides -->
            <div class="card">
                <h2>🏦 Télécharger depuis votre banque</h2>
                <details class="bank-guide">
                    <summary>📖 Voir les guides par banque</summary>
                    <div style="margin-top: 15px;">
                        <p><strong>Crédit Agricole :</strong> Compte → Télécharger opérations → CSV</p>
                        <p><strong>BNP Paribas :</strong> Mes comptes → Exporter → CSV</p>
                        <p><strong>Société Générale :</strong> Export des opérations → CSV</p>
                        <p><strong>Boursorama :</strong> Historique → Télécharger → CSV</p>
                    </div>
                </details>
            </div>
        </div>
    </div>

    <style>
        .imports-list {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .import-item {
            display: flex;
            gap: 15px;
            padding: 15px;
            border: 2px solid #e5e7eb;
            border-radius: 8px;
            background: #fafafa;
        }

        .import-item.status-completed {
            border-color: #10b981;
            background: #f0fdf4;
        }

        .import-item.status-error {
            border-color: #ef4444;
            background: #fef2f2;
        }

        .import-icon {
            font-size: 2rem;
        }

        .import-info {
            flex: 1;
        }

        .import-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
        }

        .import-date {
            color: #888;
            font-size: 0.9rem;
        }

        .import-stats {
            display: flex;
            gap: 15px;
            font-size: 0.9rem;
        }

        .stat-success {
            color: #10b981;
            font-weight: 600;
        }

        .stat-error {
            color: #ef4444;
            font-weight: 600;
        }

        .stat-pending {
            color: #f59e0b;
            font-weight: 600;
        }

        .import-error {
            margin-top: 8px;
            padding: 8px;
            background: #fee2e2;
            border-radius: 4px;
            color: #991b1b;
            font-size: 0.9rem;
        }

        .bank-guide summary {
            cursor: pointer;
            color: #6366f1;
            font-weight: 600;
            padding: 10px;
        }

        .bank-guide summary:hover {
            background: #f3f4f6;
            border-radius: 4px;
        }
    </style>
</body>
</html>