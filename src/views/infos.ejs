<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ℹ️ Mes Informations | Fidess</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 40px 0;
      font-family: 'Inter', system-ui, -apple-system, sans-serif;
    }
    
    .container-infos {
      max-width: 1200px;
      margin: 0 auto;
    }
    
    .header-infos {
      text-align: center;
      color: white;
      margin-bottom: 40px;
    }
    
    .header-infos h1 {
      font-size: 2.5rem;
      font-weight: 800;
      margin-bottom: 10px;
    }
    
    .info-box {
      background: white;
      border-radius: 20px;
      padding: 30px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.1);
      margin-bottom: 30px;
    }
    
    .info-title {
      font-size: 1.5rem;
      font-weight: 700;
      color: #2d3748;
      margin-bottom: 20px;
      display: flex;
      align-items: center;
      gap: 10px;
    }
    
    /* Badges section */
    .badges-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
      gap: 20px;
      margin-top: 20px;
    }
    
    .badge-item {
      background: #f8f9fa;
      border-radius: 15px;
      padding: 20px;
      text-align: center;
      position: relative;
      transition: all 0.3s ease;
    }
    
    .badge-item:hover {
      transform: translateY(-5px);
      box-shadow: 0 5px 20px rgba(0,0,0,0.1);
    }
    
    .badge-item.unlocked {
      background: linear-gradient(135deg, #667eea20 0%, #764ba220 100%);
      border: 2px solid #667eea;
    }
    
    .badge-emoji {
      font-size: 4rem;
      margin-bottom: 10px;
      opacity: 0.3;
    }
    
    .badge-item.unlocked .badge-emoji {
      opacity: 1;
      animation: bounce 2s infinite;
    }
    
    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }
    
    .badge-name {
      font-weight: 600;
      color: #2d3748;
      margin-bottom: 5px;
    }
    
    .badge-level {
      font-size: 0.8rem;
      color: #6c757d;
    }
    
    .badge-count {
      position: absolute;
      top: 10px;
      right: 10px;
      background: #667eea;
      color: white;
      border-radius: 50%;
      width: 30px;
      height: 30px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 700;
      font-size: 0.9rem;
    }
    
    /* Projets section */
    .projet-card {
      background: #f8f9fa;
      border-radius: 15px;
      padding: 20px;
      margin-bottom: 15px;
      border: 2px solid transparent;
      transition: all 0.3s ease;
    }
    
    .projet-card:hover {
      transform: translateX(5px);
      border-color: #667eea;
    }
    
    .projet-card.active {
      background: linear-gradient(135deg, #667eea20 0%, #764ba220 100%);
      border: 2px solid #667eea;
    }
    
    .projet-card.completed {
      background: linear-gradient(135deg, #28a74520 0%, #20c99720 100%);
      border: 2px solid #28a745;
    }
    
    .projet-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 10px;
    }
    
    .projet-nom {
      font-size: 1.3rem;
      font-weight: 700;
      color: #2d3748;
      display: flex;
      align-items: center;
      gap: 10px;
    }
    
    .badge-actif {
      background: linear-gradient(135deg, #48bb78 0%, #38a169 100%);
      color: white;
      padding: 5px 15px;
      border-radius: 20px;
      font-size: 0.85rem;
      font-weight: 600;
    }
    
    .badge-complete {
      background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
      color: white;
      padding: 5px 15px;
      border-radius: 20px;
      font-size: 0.85rem;
      font-weight: 600;
    }
    
    .projet-montant {
      font-size: 1.8rem;
      font-weight: 900;
      color: #667eea;
      font-family: 'Courier New', monospace;
      margin-bottom: 10px;
    }
    
    .projet-description {
      color: #6c757d;
      font-size: 0.95rem;
      margin-bottom: 15px;
    }
    
    .projet-actions {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
    }
    
    .btn-validate {
      background: linear-gradient(135deg, #ffc107 0%, #ff9800 100%);
      color: white;
      border: none;
      font-weight: 600;
    }
    
    .btn-validate:hover {
      background: linear-gradient(135deg, #ff9800 0%, #f57c00 100%);
      color: white;
    }
    
    .form-label {
      font-weight: 600;
      color: #4a5568;
      margin-bottom: 8px;
    }
    
    .form-control {
      border-radius: 10px;
      border: 2px solid #e2e8f0;
      padding: 12px;
      transition: all 0.3s ease;
    }
    
    .form-control:focus {
      border-color: #667eea;
      box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
    }
    
    .btn-primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border: none;
      border-radius: 10px;
      padding: 12px 30px;
      font-weight: 600;
      transition: transform 0.2s ease;
    }
    
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 20px rgba(102, 126, 234, 0.4);
    }
    
    .btn-secondary, .btn-success, .btn-danger {
      border-radius: 10px;
      font-weight: 600;
    }
    
    .alert {
      border-radius: 10px;
      border: none;
      margin-bottom: 20px;
    }
    
    .empty-state {
      text-align: center;
      padding: 40px;
      color: #6c757d;
    }
    
    .empty-state i {
      font-size: 4rem;
      margin-bottom: 20px;
      opacity: 0.3;
    }
  </style>
</head>
<body>
  <%- include('partials/navbar-simple') %>
  
  <div class="container-infos">
    
    <!-- En-tête -->
    <div class="header-infos">
      <h1>ℹ️ Mes Informations</h1>
      <p>Gérez votre profil, vos projets et vos badges</p>
    </div>
    
    <!-- Messages flash -->
    <% if (success) { %>
      <div class="alert alert-success">
        <i class="fas fa-check-circle"></i> <%= success %>
      </div>
    <% } %>
    
    <% if (error) { %>
      <div class="alert alert-danger">
        <i class="fas fa-exclamation-circle"></i> <%= error %>
      </div>
    <% } %>
    
    <!-- Ma collection de badges -->
    <div class="info-box">
      <div class="info-title">
        <i class="fas fa-trophy"></i>
        Ma collection de badges
      </div>
      
      <% if (userBadges && userBadges.length > 0) { %>
        <div class="badges-grid">
          <% allBadges.forEach(badge => { %>
            <div class="badge-item <%= badgeCounts[badge.niveau] ? 'unlocked' : '' %>">
              <% if (badgeCounts[badge.niveau]) { %>
                <div class="badge-count"><%= badgeCounts[badge.niveau] %></div>
              <% } %>
              <div class="badge-emoji"><%= badge.emoji %></div>
              <div class="badge-name"><%= badge.nom %></div>
              <div class="badge-level">Niveau <%= badge.niveau %></div>
            </div>
          <% }) %>
        </div>
      <% } else { %>
        <div class="empty-state">
          <i class="fas fa-trophy"></i>
          <p>Vous n'avez pas encore de badge.</p>
          <p class="text-muted">Complétez votre premier projet pour débloquer un badge !</p>
        </div>
      <% } %>
    </div>
    
    <!-- Projets complétés -->
    <% if (projetsCompletes && projetsCompletes.length > 0) { %>
      <div class="info-box">
        <div class="info-title">
          <i class="fas fa-check-circle"></i>
          Projets réalisés (<%= projetsCompletes.length %>)
        </div>
        
        <% projetsCompletes.forEach(projet => { %>
          <div class="projet-card completed">
            <div class="projet-header">
              <div class="projet-nom">
                <%= projet.nom %>
                <span class="badge-complete">✓ RÉALISÉ</span>
              </div>
            </div>
            
            <div class="projet-montant">
              🎯 <%= parseFloat(projet.montant_objectif).toLocaleString('fr-FR') %>€
            </div>
            
            <% if (projet.description) { %>
              <div class="projet-description">
                <%= projet.description %>
              </div>
            <% } %>
            
            <div style="font-size: 0.9rem; color: #28a745; font-weight: 600;">
              <i class="fas fa-calendar-check"></i> 
              Complété le <%= new Date(projet.date_completion).toLocaleDateString('fr-FR') %>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>
    
    <!-- Informations personnelles -->
    <div class="info-box">
      <div class="info-title">
        <i class="fas fa-user"></i>
        Informations personnelles
      </div>
      
      <form action="/infos/update-profile" method="POST">
        <div class="row">
          <div class="col-md-6 mb-3">
            <label class="form-label">Nom *</label>
            <input type="text" class="form-control" name="nom" value="<%= user.nom || '' %>" required>
          </div>
          
          <div class="col-md-6 mb-3">
            <label class="form-label">Prénom *</label>
            <input type="text" class="form-control" name="prenom" value="<%= user.prenom || '' %>" required>
          </div>
        </div>
        
        <div class="row">
          <div class="col-md-6 mb-3">
            <label class="form-label">Pseudo *</label>
            <input type="text" class="form-control" name="pseudo" value="<%= user.pseudo || '' %>" required>
          </div>
          
          <div class="col-md-6 mb-3">
            <label class="form-label">Email</label>
            <input type="email" class="form-control" value="<%= user.adresse_mail || '' %>" disabled>
            <small class="text-muted">L'email ne peut pas être modifié</small>
          </div>
        </div>
        
        <div class="row">
          <div class="col-md-4 mb-3">
            <label class="form-label">Date de naissance</label>
            <input type="date" class="form-control" name="date_de_naissance" value="<%= user.date_de_naissance || '' %>">
          </div>
          
          <div class="col-md-4 mb-3">
            <label class="form-label">Situation professionnelle</label>
            <input type="text" class="form-control" name="situation_professionnelle" value="<%= user.situation_professionnelle || '' %>" placeholder="Ex: Salarié, Étudiant...">
          </div>
          
          <div class="col-md-4 mb-3">
            <label class="form-label">Ville</label>
            <input type="text" class="form-control" name="ville" value="<%= user.ville || '' %>" placeholder="Ex: Paris">
          </div>
        </div>
        
        <div class="text-end">
          <button type="submit" class="btn btn-primary">
            <i class="fas fa-save"></i> Enregistrer les modifications
          </button>
        </div>
      </form>
    </div>
    
    <!-- Mes projets financiers -->
    <div class="info-box">
      <div class="info-title">
        <i class="fas fa-bullseye"></i>
        Mes projets financiers
      </div>
      
      <!-- Liste des projets actifs -->
      <% if (projetsActifs && projetsActifs.length > 0) { %>
        <% projetsActifs.forEach(projet => { %>
          <div class="projet-card <%= projet.est_actif ? 'active' : '' %>">
            <div class="projet-header">
              <div class="projet-nom">
                <%= projet.nom %>
                <% if (projet.est_actif) { %>
                  <span class="badge-actif">✓ ACTIF</span>
                <% } %>
              </div>
            </div>
            
            <div class="projet-montant">
              🎯 <%= parseFloat(projet.montant_objectif).toLocaleString('fr-FR') %>€
            </div>
            
            <% if (projet.description) { %>
              <div class="projet-description">
                <%= projet.description %>
              </div>
            <% } %>
            
            <div class="projet-actions">
              <!-- Bouton Valider le projet -->
              <a href="/projet/valider/<%= projet.id %>" class="btn btn-validate btn-sm">
                <i class="fas fa-check-circle"></i> Valider le projet
              </a>
              
              <% if (!projet.est_actif) { %>
                <form action="/infos/projet/set-active/<%= projet.id %>" method="POST" style="display: inline;">
                  <button type="submit" class="btn btn-success btn-sm">
                    <i class="fas fa-check"></i> Activer ce projet
                  </button>
                </form>
              <% } %>
              
              <button class="btn btn-secondary btn-sm" onclick="editProjet('<%= projet.id %>', '<%= projet.nom %>', '<%= projet.montant_objectif %>', '<%= projet.description || '' %>')">
                <i class="fas fa-edit"></i> Modifier
              </button>
              
              <form action="/infos/projet/delete/<%= projet.id %>" method="POST" style="display: inline;" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce projet ?')">
                <button type="submit" class="btn btn-danger btn-sm">
                  <i class="fas fa-trash"></i> Supprimer
                </button>
              </form>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <div class="empty-state">
          <i class="fas fa-inbox"></i>
          <p>Vous n'avez pas encore de projet financier.</p>
          <p class="text-muted">Créez votre premier projet pour commencer à suivre vos objectifs !</p>
        </div>
      <% } %>
      
      <!-- Formulaire nouveau projet -->
      <div class="mt-4">
        <button class="btn btn-primary" data-bs-toggle="collapse" data-bs-target="#newProjetForm">
          <i class="fas fa-plus"></i> Créer un nouveau projet
        </button>
        
        <div class="collapse mt-3" id="newProjetForm">
          <div class="card card-body" style="border-radius: 15px; border: 2px solid #e2e8f0;">
            <form action="/infos/projet/create" method="POST">
              <div class="mb-3">
                <label class="form-label">Nom du projet *</label>
                <input type="text" class="form-control" name="nom" placeholder="Ex: Acheter un vélo Gravel" required>
              </div>
              
              <div class="mb-3">
                <label class="form-label">Montant objectif (€) *</label>
                <input type="number" class="form-control" name="montant_objectif" placeholder="Ex: 565" step="0.01" min="0" required>
              </div>
              
              <div class="mb-3">
                <label class="form-label">Description (optionnel)</label>
                <textarea class="form-control" name="description" rows="3" placeholder="Décrivez votre projet..."></textarea>
              </div>
              
              <div class="text-end">
                <button type="button" class="btn btn-secondary" data-bs-toggle="collapse" data-bs-target="#newProjetForm">Annuler</button>
                <button type="submit" class="btn btn-primary">
                  <i class="fas fa-save"></i> Créer le projet
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
    
  </div>
  
  <!-- Modal édition projet -->
  <div class="modal fade" id="editProjetModal" tabindex="-1">
    <div class="modal-dialog">
      <div class="modal-content" style="border-radius: 20px;">
        <div class="modal-header">
          <h5 class="modal-title">Modifier le projet</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>
        <form id="editProjetForm" method="POST">
          <div class="modal-body">
            <div class="mb-3">
              <label class="form-label">Nom du projet *</label>
              <input type="text" class="form-control" name="nom" id="edit_nom" required>
            </div>
            
            <div class="mb-3">
              <label class="form-label">Montant objectif (€) *</label>
              <input type="number" class="form-control" name="montant_objectif" id="edit_montant" step="0.01" min="0" required>
            </div>
            
            <div class="mb-3">
              <label class="form-label">Description</label>
              <textarea class="form-control" name="description" id="edit_description" rows="3"></textarea>
            </div>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
            <button type="submit" class="btn btn-primary">
              <i class="fas fa-save"></i> Enregistrer
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    function editProjet(id, nom, montant, description) {
      document.getElementById('editProjetForm').action = `/infos/projet/update/${id}`;
      document.getElementById('edit_nom').value = nom;
      document.getElementById('edit_montant').value = montant;
      document.getElementById('edit_description').value = description;
      
      const modal = new bootstrap.Modal(document.getElementById('editProjetModal'));
      modal.show();
    }
  </script>
</body>
</html>
