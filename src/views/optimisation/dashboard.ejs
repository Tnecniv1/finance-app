<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tableau de bord budgétaire | Fidess</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 40px 0;
      font-family: 'Inter', system-ui, -apple-system, sans-serif;
    }
    
    .container-dashboard {
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 20px;
    }
    
    .header {
      background: white;
      border-radius: 20px;
      padding: 30px;
      margin-bottom: 30px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.1);
    }
    
    .header h1 {
      color: #667eea;
      margin: 0;
      font-size: 2rem;
      font-weight: 700;
    }
    
    .mois-actuel {
      color: #6c757d;
      font-size: 1.1rem;
      margin-top: 5px;
    }
    
    .bloc {
      background: white;
      border-radius: 20px;
      padding: 30px;
      margin-bottom: 30px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.1);
    }
    
    .bloc-title {
      font-size: 1.5rem;
      font-weight: 700;
      color: #2d3748;
      margin-bottom: 20px;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    /* Tableau budgétaire */
    .budget-table-container {
      overflow-x: auto;
      margin-top: 20px;
    }

    .budget-table {
      width: 100%;
      border-collapse: collapse;
      font-size: 0.95rem;
    }

    .budget-table thead th {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 15px 12px;
      text-align: center;
      font-weight: 700;
      position: sticky;
      top: 0;
      z-index: 10;
      border: 1px solid rgba(255,255,255,0.1);
    }

    .budget-table thead th:first-child {
      text-align: left;
      width: 20%;
    }

    .budget-table thead th.delta-col {
      background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      width: 10%;
    }

    .budget-table thead th.revenus-col {
      background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
    }

    .budget-table thead th.depenses-col {
      background: linear-gradient(135deg, #dc3545 0%, #fd7e14 100%);
    }

    .budget-table tbody tr {
      border-bottom: 1px solid #e9ecef;
      transition: background-color 0.2s;
    }

    .budget-table tbody tr:hover {
      background: #f8f9fa;
    }

    .budget-table tbody tr.total-row {
      background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
      font-weight: 700;
      font-size: 1.05rem;
      border-top: 3px solid #667eea;
    }

    .budget-table tbody tr.total-row:hover {
      background: linear-gradient(135deg, #e9ecef 0%, #dee2e6 100%);
    }

    .budget-table tbody td {
      padding: 12px;
      text-align: center;
      font-family: 'Courier New', monospace;
    }

    .budget-table tbody td:first-child {
      text-align: left;
      font-family: 'Inter', system-ui, -apple-system, sans-serif;
      font-weight: 600;
      color: #2d3748;
    }

    .montant-estime {
      color: #6c757d;
      font-weight: 500;
    }

    .montant-observe {
      font-weight: 700;
      color: #2d3748;
    }

    .montant-observe.revenus {
      color: #28a745;
    }

    .montant-observe.depenses {
      color: #dc3545;
    }

    .montant-zero {
      color: #cbd5e0;
    }

    .delta-positif {
      background: #d4f4dd;
      color: #28a745;
      font-weight: 700;
    }

    .delta-negatif {
      background: #ffe5e5;
      color: #dc3545;
      font-weight: 700;
    }

    .delta-neutre {
      color: #6c757d;
    }

    .depassement {
      background: #fff5f5;
      color: #dc3545;
      font-weight: 700;
    }

    .sous-budget {
      background: #f0fdf4;
      color: #28a745;
      font-weight: 700;
    }

    /* Résumé en haut du tableau */
    .resume-budget {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 15px;
      margin-bottom: 25px;
    }

    .resume-card {
      background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
      border-radius: 12px;
      padding: 20px;
      text-align: center;
      border: 2px solid #e9ecef;
    }

    .resume-card.revenus {
      background: linear-gradient(135deg, #d4f4dd 0%, #b7f0c3 100%);
      border-color: #28a745;
    }

    .resume-card.depenses {
      background: linear-gradient(135deg, #ffe5e5 0%, #ffd1d1 100%);
      border-color: #dc3545;
    }

    .resume-label {
      font-size: 0.85rem;
      color: #6c757d;
      margin-bottom: 8px;
      font-weight: 600;
    }

    .resume-montant {
      font-size: 1.8rem;
      font-weight: 900;
      font-family: 'Courier New', monospace;
      color: #2d3748;
    }

    .resume-montant.revenus {
      color: #28a745;
    }

    .resume-montant.depenses {
      color: #dc3545;
    }

    /* Bouton */
    .btn-faire-budget {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border: none;
      color: white;
      padding: 20px 40px;
      font-size: 1.3rem;
      font-weight: 700;
      border-radius: 15px;
      width: 100%;
      transition: all 0.3s ease;
      box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
    }

    .btn-faire-budget:hover {
      transform: translateY(-2px);
      box-shadow: 0 15px 40px rgba(102, 126, 234, 0.4);
    }

    .alert-info-custom {
      background: #e7f3ff;
      border: 2px solid #667eea;
      border-radius: 15px;
      padding: 20px;
      margin-bottom: 20px;
    }

    .alert-info-custom i {
      color: #667eea;
      font-size: 1.5rem;
    }

    .text-center {
      text-align: center;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .resume-budget {
        grid-template-columns: 1fr 1fr;
      }

      .budget-table {
        font-size: 0.85rem;
      }

      .budget-table thead th,
      .budget-table tbody td {
        padding: 8px 6px;
      }

      .resume-montant {
        font-size: 1.4rem;
      }
    }
  </style>
</head>
<body>
  <%- include('../partials/navbar-simple') %>
  
  <div class="container-dashboard">
    
    <!-- En-tête -->
    <div class="header">
      <h1><i class="fas fa-chart-line"></i> Tableau de bord budgétaire</h1>
      <p class="mois-actuel"><i class="far fa-calendar"></i> <%= moisActuel %></p>
    </div>
    
    <!-- BLOC 1 : Tableau budgétaire -->
    <div class="bloc">
      <h2 class="bloc-title">
        <i class="fas fa-table"></i> Budget du mois
      </h2>
      
      <% if (!dernierPlan) { %>
        <div class="alert-info-custom">
          <i class="fas fa-info-circle"></i>
          <strong>Aucun budget défini</strong><br>
          Vous n'avez pas encore créé de budget. Les montants observés représentent vos transactions actuelles du mois.
        </div>
      <% } %>

      <!-- Résumé en cartes -->
      <div class="resume-budget">
        <div class="resume-card revenus">
          <div class="resume-label"><i class="fas fa-arrow-up"></i> Revenus estimés</div>
          <div class="resume-montant revenus"><%= totaux.revenus.budget.toFixed(0) %>€</div>
        </div>
        <div class="resume-card revenus">
          <div class="resume-label"><i class="fas fa-check-circle"></i> Revenus observés</div>
          <div class="resume-montant revenus"><%= totaux.revenus.actuel.toFixed(0) %>€</div>
        </div>
        <div class="resume-card depenses">
          <div class="resume-label"><i class="fas fa-arrow-down"></i> Dépenses estimées</div>
          <div class="resume-montant depenses"><%= totaux.depenses.budget.toFixed(0) %>€</div>
        </div>
        <div class="resume-card depenses">
          <div class="resume-label"><i class="fas fa-exclamation-circle"></i> Dépenses observées</div>
          <div class="resume-montant depenses"><%= totaux.depenses.actuel.toFixed(0) %>€</div>
        </div>
      </div>

      <!-- Tableau détaillé -->
      <div class="budget-table-container">
        <table class="budget-table">
          <thead>
            <tr>
              <th>Catégorie</th>
              <th class="delta-col">Delta</th>
              <th class="revenus-col">Revenus estimés</th>
              <th class="revenus-col">Revenus observés</th>
              <th class="depenses-col">Dépenses estimées</th>
              <th class="depenses-col">Dépenses observées</th>
            </tr>
          </thead>
          <tbody>
            <% 
            // ✅ CORRECTION : Ne PAS fusionner les taxonomies
            // On affiche d'abord toutes les catégories de REVENUS
            // Puis toutes les catégories de DÉPENSES
            
            // Trier les revenus
            const revenusTriees = tableauComparatif.revenus.sort((a, b) => a.nom.localeCompare(b.nom));
            
            // Trier les dépenses
            const depensesTriees = tableauComparatif.depenses.sort((a, b) => a.nom.localeCompare(b.nom));
            
            // Afficher d'abord toutes les catégories de REVENUS
            revenusTriees.forEach(revenu => {
              const classRevenuObserve = revenu.montantActuel === 0 ? 'montant-zero' : 'montant-observe revenus';
              
              // Calcul du delta pour les revenus : observé - estimé
              const delta = revenu.montantActuel - revenu.budgetFixe;
              const deltaTxt = delta === 0 ? '—' : (delta > 0 ? '+' : '') + delta.toFixed(0) + '€';
              const classDelta = delta === 0 ? 'delta-neutre' : (delta >= 0 ? 'delta-positif' : 'delta-negatif');
            %>
              <tr>
                <td><%= revenu.nom %></td>
                <td class="<%= classDelta %>"><%= deltaTxt %></td>
                <td class="montant-estime"><%= revenu.budgetFixe.toFixed(0) %>€</td>
                <td class="<%= classRevenuObserve %>"><%= revenu.montantActuel.toFixed(0) %>€</td>
                <td class="montant-zero">—</td>
                <td class="montant-zero">—</td>
              </tr>
            <% }); %>
            
            <!-- Séparateur visuel entre revenus et dépenses -->
            <tr style="height: 10px; background: #e9ecef;">
              <td colspan="6"></td>
            </tr>
            
            <% 
            // Afficher ensuite toutes les catégories de DÉPENSES
            depensesTriees.forEach(depense => {
              const classDepenseObservee = depense.montantActuel === 0 ? 'montant-zero' : 
                (depense.budgetFixe > 0 && depense.montantActuel > depense.budgetFixe) ? 'montant-observe depenses depassement' : 
                'montant-observe depenses';
              
              // Calcul du delta pour les dépenses : estimé - observé (inversé car moins de dépenses = mieux)
              const delta = depense.budgetFixe - depense.montantActuel;
              const deltaTxt = delta === 0 ? '—' : (delta > 0 ? '+' : '') + delta.toFixed(0) + '€';
              const classDelta = delta === 0 ? 'delta-neutre' : (delta > 0 ? 'delta-positif' : 'delta-negatif');
            %>
              <tr>
                <td><%= depense.nom %></td>
                <td class="<%= classDelta %>"><%= deltaTxt %></td>
                <td class="montant-zero">—</td>
                <td class="montant-zero">—</td>
                <td class="montant-estime"><%= depense.budgetFixe.toFixed(0) %>€</td>
                <td class="<%= classDepenseObservee %>"><%= depense.montantActuel.toFixed(0) %>€</td>
              </tr>
            <% }); %>
            
            <!-- Ligne de total -->
            <% 
              const deltaRevenus = totaux.revenus.actuel - totaux.revenus.budget;
              const deltaDepenses = totaux.depenses.budget - totaux.depenses.actuel;
              const deltaTotal = deltaRevenus + deltaDepenses;
              const classeDeltaTotal = deltaTotal === 0 ? 'delta-neutre' : (deltaTotal > 0 ? 'delta-positif' : 'delta-negatif');
            %>
            <tr class="total-row">
              <td><strong>TOTAL</strong></td>
              <td class="<%= classeDeltaTotal %>">
                <strong><%= deltaTotal > 0 ? '+' : '' %><%= deltaTotal.toFixed(0) %>€</strong>
              </td>
              <td><strong><%= totaux.revenus.budget.toFixed(0) %>€</strong></td>
              <td><strong><%= totaux.revenus.actuel.toFixed(0) %>€</strong></td>
              <td><strong><%= totaux.depenses.budget.toFixed(0) %>€</strong></td>
              <td><strong><%= totaux.depenses.actuel.toFixed(0) %>€</strong></td>
            </tr>
            
            <!-- Ligne solde -->
            <% 
              const soldeEstime = totaux.revenus.budget - totaux.depenses.budget;
              const soldeObserve = totaux.revenus.actuel - totaux.depenses.actuel;
              const classeSoldeEstime = soldeEstime >= 0 ? 'sous-budget' : 'depassement';
              const classeSoldeObserve = soldeObserve >= 0 ? 'sous-budget' : 'depassement';
            %>
            <tr class="total-row">
              <td><strong>💰 SOLDE</strong></td>
              <td></td>
              <td colspan="2" class="<%= classeSoldeEstime %>">
                <strong><%= soldeEstime >= 0 ? '+' : '' %><%= soldeEstime.toFixed(0) %>€</strong>
              </td>
              <td colspan="2" class="<%= classeSoldeObserve %>">
                <strong><%= soldeObserve >= 0 ? '+' : '' %><%= soldeObserve.toFixed(0) %>€</strong>
              </td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
    
    <!-- BLOC 2 : Faire ses budgets -->
    <div class="bloc">
      <h2 class="bloc-title">
        <i class="fas fa-bullseye"></i> Planifier mon budget
      </h2>
      
      <p style="color: #6c757d; margin-bottom: 25px;">
        <% if (dernierPlan) { %>
          Créez un nouveau budget ou ajustez votre budget existant pour le mois prochain.
        <% } else { %>
          Créez votre premier budget personnalisé en 3 étapes simples.
        <% } %>
      </p>
      
      <a href="/optimisation/nouveau" class="btn btn-faire-budget">
        <i class="fas fa-rocket"></i> <%= dernierPlan ? 'Créer un nouveau budget' : 'Démarrer mon premier budget' %>
      </a>
      
      <div class="text-center mt-3">
        <a href="/transactions" style="color: #667eea; text-decoration: none;">
          <i class="fas fa-arrow-left"></i> Retour aux transactions
        </a>
      </div>
    </div>
    
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
