<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Historique des optimisations | Finance App</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    body {
      background-color: #f8f9fa;
      padding: 20px 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .container {
      max-width: 1200px;
    }
    .page-header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 40px 30px;
      border-radius: 15px;
      margin-bottom: 30px;
      box-shadow: 0 5px 20px rgba(0,0,0,0.1);
    }
    .page-header h1 {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 10px;
    }
    .page-header p {
      font-size: 1.1rem;
      opacity: 0.9;
      margin: 0;
    }
    .btn-back {
      background: white;
      color: #667eea;
      border: none;
      padding: 10px 20px;
      border-radius: 50px;
      font-weight: 600;
      transition: all 0.3s ease;
      display: inline-flex;
      align-items: center;
      gap: 8px;
      margin-top: 15px;
    }
    .btn-back:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(255,255,255,0.3);
    }
    .session-card {
      background: white;
      border-radius: 15px;
      box-shadow: 0 3px 15px rgba(0,0,0,0.08);
      margin-bottom: 20px;
      overflow: hidden;
      transition: all 0.3s ease;
    }
    .session-card:hover {
      box-shadow: 0 5px 25px rgba(0,0,0,0.12);
      transform: translateY(-3px);
    }
    .session-header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 20px 25px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .session-mois {
      font-size: 1.3rem;
      font-weight: 700;
    }
    .session-date {
      font-size: 0.9rem;
      opacity: 0.9;
    }
    .session-body {
      padding: 25px;
    }
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 20px;
      margin-bottom: 20px;
    }
    .stat-box {
      text-align: center;
      padding: 15px;
      background: #f8f9fa;
      border-radius: 10px;
    }
    .stat-label {
      font-size: 0.9rem;
      color: #6c757d;
      margin-bottom: 8px;
    }
    .stat-value {
      font-size: 1.8rem;
      font-weight: 700;
    }
    .stat-value.revenus {
      color: #28a745;
    }
    .stat-value.depenses {
      color: #dc3545;
    }
    .stat-value.solde {
      color: #667eea;
    }
    .stat-value.economie {
      color: #20c997;
    }
    .solde-badge {
      display: inline-block;
      padding: 8px 20px;
      border-radius: 50px;
      font-weight: 700;
      font-size: 1.2rem;
    }
    .solde-badge.positif {
      background: #d4edda;
      color: #155724;
    }
    .solde-badge.negatif {
      background: #f8d7da;
      color: #721c24;
    }
    .actions-buttons {
      display: flex;
      gap: 10px;
      justify-content: flex-end;
      margin-top: 20px;
      padding-top: 20px;
      border-top: 2px solid #e9ecef;
    }
    .btn-view {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 50px;
      font-weight: 600;
      transition: all 0.3s ease;
    }
    .btn-view:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
    }
    .empty-state {
      background: white;
      border-radius: 15px;
      padding: 80px 40px;
      text-align: center;
      box-shadow: 0 3px 15px rgba(0,0,0,0.08);
    }
    .empty-state i {
      font-size: 5rem;
      color: #e9ecef;
      margin-bottom: 25px;
    }
    .empty-state h3 {
      color: #495057;
      margin-bottom: 15px;
    }
    .empty-state p {
      color: #6c757d;
      margin-bottom: 25px;
    }
    .btn-start {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      padding: 15px 40px;
      border-radius: 50px;
      font-weight: 600;
      font-size: 1.1rem;
      box-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
      transition: all 0.3s ease;
    }
    .btn-start:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(102, 126, 234, 0.4);
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- En-tête -->
    <div class="page-header">
      <h1><i class="fas fa-history"></i> Historique des optimisations</h1>
      <p>Consultez vos précédentes sessions d'optimisation budgétaire</p>
      <a href="/transactions" class="btn-back">
        <i class="fas fa-arrow-left"></i> Retour aux transactions
      </a>
    </div>
    
    <!-- Liste des sessions -->
    <% if (sessions.length === 0) { %>
      <!-- État vide -->
      <div class="empty-state">
        <i class="fas fa-clipboard-list"></i>
        <h3>Aucune optimisation validée</h3>
        <p>Vous n'avez pas encore complété de session d'optimisation budgétaire.</p>
        <a href="/optimisation/start" class="btn-start">
          <i class="fas fa-rocket"></i> Démarrer ma première optimisation
        </a>
      </div>
    <% } else { %>
      <!-- Sessions existantes -->
      <% sessions.forEach(session => { 
        const moisFormatted = new Date(session.mois_cible).toLocaleDateString('fr-FR', { 
          month: 'long', 
          year: 'numeric' 
        });
        const dateValidation = new Date(session.validated_at).toLocaleDateString('fr-FR', {
          day: '2-digit',
          month: '2-digit',
          year: 'numeric'
        });
        const soldePositif = parseFloat(session.solde_previsionnel) >= 0;
      %>
        <div class="session-card">
          <div class="session-header">
            <div>
              <div class="session-mois">📅 <%= moisFormatted %></div>
              <div class="session-date">Validé le <%= dateValidation %></div>
            </div>
            <div>
              <span class="solde-badge <%= soldePositif ? 'positif' : 'negatif' %>">
                <%= soldePositif ? '+' : '' %><%= parseFloat(session.solde_previsionnel).toFixed(2) %>€
              </span>
            </div>
          </div>
          
          <div class="session-body">
            <!-- Statistiques -->
            <div class="stats-grid">
              <div class="stat-box">
                <div class="stat-label">💰 Revenus</div>
                <div class="stat-value revenus">
                  +<%= parseFloat(session.revenus_optimises).toFixed(2) %>€
                </div>
              </div>
              <div class="stat-box">
                <div class="stat-label">💸 Dépenses</div>
                <div class="stat-value depenses">
                  -<%= parseFloat(session.depenses_optimisees).toFixed(2) %>€
                </div>
              </div>
              <div class="stat-box">
                <div class="stat-label">💵 Solde</div>
                <div class="stat-value solde">
                  <%= soldePositif ? '+' : '' %><%= parseFloat(session.solde_previsionnel).toFixed(2) %>€
                </div>
              </div>
              <div class="stat-box">
                <div class="stat-label">💎 Économies</div>
                <div class="stat-value economie">
                  <%= (parseFloat(session.revenus_recurrents) - parseFloat(session.revenus_optimises) + 
                       parseFloat(session.depenses_recurrentes) - parseFloat(session.depenses_optimisees)).toFixed(2) %>€
                </div>
              </div>
            </div>
            
            <!-- Boutons d'action -->
            <div class="actions-buttons">
              <a href="/optimisation/rectangle/<%= session.id %>" class="btn-view" target="_blank">
                <i class="fas fa-print"></i> Voir le plan
              </a>
            </div>
          </div>
        </div>
      <% }) %>
      
      <!-- Bouton nouvelle optimisation -->
      <div class="text-center mt-4">
        <a href="/optimisation/start" class="btn-start">
          <i class="fas fa-plus-circle"></i> Nouvelle optimisation
        </a>
      </div>
    <% } %>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
